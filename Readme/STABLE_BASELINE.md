# Стабильная baseline (заморозка)

**Актуальная веха (таблица мечт + сопутствующий UI):** **app-version 161** в `index.html` (футер кабинета).

**Дата фиксации контракта таблицы:** 2026-04-18 (итерации colgroup, § 1.1, колонка «Статус» **8.5em** — три контролла в ряд).

Команда договорилась считать **текущую** ревизию **стабильной** для дальнейшей разработки (с учётом известных проблем **мобильной** вёрстки ЛК в целом — отдельная линия работ, не отменяет фиксацию десктоп-таблицы).

## Где смотреть норму по таблице мечт

- **Смысл и цифры:** [Readme/UI-standards.md](UI-standards.md) — **§ 1.1** (макет колонок).
- **HTML/CSS-снимок для копипаста:** [Readme/stable/dreams_table.html](stable/dreams_table.html) + [Readme/stable/README.md](stable/README.md).

## Для сравнения с продом

После деплоя: значение `.app-version` в футере = **161** (или выше, если выкатили следующий релиз).

## Git-тег (по желанию, после `git pull` на `main`)

```bash
git tag -a stable-dreams-table-2026-04 -m "Dreams table contract §1.1, app 161, colgroup + status 8.5em"
git push origin stable-dreams-table-2026-04
```

Ранее: `stable-2026-04-18` — ранняя веха; при необходимости оставляем в истории, **не** перетирая без согласования.

## Содержание вехи (кратко)

- **Таблица «Мои мечты» / «Мечты бадди»:** `colgroup` + фиксированные колонки (№, Шаги, Дедлайн, Цена, Статус), **Мечта** = остаток ширины; **Шаги** — счёт и прогресс фоном ячейки; **Статус** **8.5em** (глаз + пикер + карандаш).
- **Дневник бадди** (отдельная фича): `GET /steps/events` с `user_id` владельца и `viewer_id` (см. [CHANGELOG](CHANGELOG.md)).

См. также [CHANGELOG.md](CHANGELOG.md).
