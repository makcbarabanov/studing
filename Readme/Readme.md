# 🏝️ Проект "ОСТРОВ" (The Island)

## Контекст для ассистента (AI)

**Чтобы быть в курсе проекта и давать релевантные советы** (в т.ч. про миграцию, восстановление БД, структуру приложения):

*   **При первом обращении пользователя в сессии** (или **раз в день** при длительной работе) — освежить контекст: просмотреть ключевые файлы и разделы:
    *   [Readme/Forge.md](Forge.md) — личность и роль **Форжа** (Lead Developer в Cursor) в контексте ОСТРОВа;
    *   этот файл (Readme/Readme.md) целиком;
    *   [Readme/structure.md](structure.md) — структура личного кабинета, мобильная/браузерная вёрстка;
    *   [Readme/tables.md](tables.md) — схема БД, таблицы;
    *   [roadmap.html](../roadmap.html) — планы и статусы шагов (данные из БД, таблица `roadmap`);
    *   раздел ниже «Резервное копирование БД» (папка `db_backups/`, готовые дампы для восстановления и переноса);
    *   при необходимости — обзор `main.py` (эндпоинты, подключение к БД), `db_backups/backup.log` (последние бэкапы).
    *   **Интеграция с ботом Bloom (Bridge):** живая спецификация API и общих полей БД — репозиторий [island-bridge-contract](https://github.com/makcbarabanov/island-bridge-contract) (`README.md`, `api.md`, `db-shared.md`, `runbook.md`); зеркало в репо бота: `bridge/docs/handbook/`. История изменений: [commits на main](https://github.com/makcbarabanov/island-bridge-contract/commits/main). Правило: поменял API или общие поля — обновить handbook там и в зеркале (тот же дух или следующий коммит), затем код на Острове и в боте.
*   У проекта **есть автоматический бэкап БД** в папке `db_backups/`. При советах по миграции на другой сервер или восстановлению — учитывать наличие готовых дампов (последний `.dump` можно переносить и восстанавливать через `pg_restore`), а не только «сделать pg_dump с текущего сервера».

### Вопросы Форжа по новой песочнице (локально + Docker)

Ниже список уточнений после переезда с удалённого сервера на локальный ноутбук Макса. После ответа Макса пункты ниже заполняются прямо в этом разделе как единый источник правды.

1. Какой теперь **канонический способ запуска** для разработки: только `docker compose up --build`, или допустим локальный `uvicorn` без Docker для отладки?
2. Нужен ли отдельный `docker-compose.override.yml` для локалки (дебаг, автоперезагрузка, проброс портов), или держим всё в одном `docker-compose.yml`?
3. В `docker-compose.yml` сейчас задан `POSTGRES_PASSWORD=твой_пароль_из_енв`. Подтверди, что пароль должен приходить только из `.env` (без хардкода в compose), и как называется финальная переменная.
4. Какой **порт приложения** считаем основным в локалке и в проде: `8000` в обоих окружениях?
5. Миграции `_sql/*.sql` теперь запускаем **из контейнера app** (`docker compose exec app python3 run_migrate.py ...`) или с хоста (`python3 run_migrate.py ...`)?
6. База в локалке инициализируется из `init.sql` автоматически при первом старте контейнера `db`, или используем отдельный шаг восстановления дампа?
7. Какой текущий источник истины для восстановления: дамп из `db_backups/*.dump`, или уже подготовленный локальный volume `postgres_data`?
8. Какой целевой путь проекта на проде после унификации: `/home/makc/Apps/island` (подтверждено)?
9. Что именно должен делать агент на проде после `git pull`: только актуализировать и перезапускать сервис, или ещё прогонять миграции/проверки?
10. Нужен ли в `README` отдельный блок «Границы ответственности агентов» (Forge/Gemini/прод-агент), чтобы исключить пересечения?

---

## 🌟 Миссия проекта
Социальная сеть для целеустремленных людей. Платформа, где мечты превращаются в реальность через поддержку окружения и декомпозицию целей.

## 🚀 Функционал (MVP)
### 1. Витрина Мечт (Dream Showcase)
*   Пользователь описывает свои желания в личном кабинете.
*   Мечты становятся публичными (витрина).
*   Другие пользователи могут видеть мечты и помогать их осуществлять (подарить, помочь ресурсами).

### 2. Путь к Цели (The Path)
*   Большая мечта разбивается на ежедневные маленькие шаги.
*   Интеграция с расписанием человека.
*   **AI-Наставник:** Бот (Telegram/App), который принимает ежедневные отчеты, отслеживает прогресс и корректирует траекторию движения.

---

## 🛠 Технический Стек (Architecture)

**Роли:**
*   **Product Owner:** Максим (Автор идеи).
*   **Chief Architect / DevOps:** AI-Наставник Сеньор Сре(Google AI).
*   **Lead Developer:** Forge (Cursor AI).

### Разделение агентов: локалка и прод

Чтобы не путать задачи и ответственность:

*   **Forge (локальный агент):** работает на ноутбуке Макса в песочнице `~/Apps/island`, делает разработку, правки кода, документацию, локальные проверки, подготовку коммитов.
*   **Продагент (серверный агент):** работает на прод-сервере в директории проекта на сервере, принимает изменения из Git (`git pull`), выполняет продовые действия (перезапуск сервиса, проверка состояния, при необходимости миграции по регламенту).

**Правило идентификации для серверного агента:** если ты читаешь этот `Readme` на проде — ты и есть **Продагент**. Действуй в рамках продовой роли, не смешивай её с задачами локальной разработки Forge.

#### Проверка роли по окружению (обязательно перед началом задачи)

**Роли взаимоисключающие. Агент не может быть одновременно Forge и Продагентом.**

1. Если текущий сервер/хост соответствует прод-окружению с IP **`188.225.44.48`** — роль агента: **Продагент**.
2. Если работа идёт в локальной песочнице Макса (проект `~/Apps/island`) и это не прод-хост `188.225.44.48` — роль агента: **Forge**.
3. Если есть сомнение:
   - проверить IP/хост машины;
   - посмотреть путь проекта и контекст терминала;
   - только после этого выбирать роль и выполнять действия.

**Приоритет правила:** проверка IP `188.225.44.48` имеет приоритет над названием агента/сессии. На этом IP агент всегда работает как **Продагент**.

**Жёсткая фиксация:**
*   На локальном компьютере Макса агент = **Forge** (и только Forge).
*   На прод-сервере `188.225.44.48` агент = **Продагент** (и только Продагент).
*   Формулировка «если по-простому зови меня Форж» на проде не применяется.
*   **Продагент не наводит “порядки” в коде на сервере** (рефакторинг, перенос файлов, переименование структуры, массовые правки README/архитектуры).
*   Любые структурные изменения делает только **Forge в песочнице**; затем изменения проходят через GitHub (`commit/push`) и только после этого подтягиваются на прод (`git pull`).
*   Продагент выполняет только продовый контур: `pull`, перезапуск сервисов, проверка состояния, применение заранее подготовленных изменений по регламенту.

#### SOP: Forge ↔ Продагент (рабочий цикл)

1. **Постановка задачи:** Макс помечает задачу как `ЛОКАЛКА/FORGE` или `ПРОД/PRODAGENT`.
2. **Проверка роли:** перед началом агент сверяет окружение (локалка vs IP `188.225.44.48`) и подтверждает единственную роль.
3. **Разработка:** любые изменения кода/структуры/README делает только Forge в `~/Apps/island`.
4. **Локальная проверка:** Forge проверяет работоспособность, обновляет документацию и счётчик версии (`.app-version`) по правилам проекта.
5. **Публикация:** Forge делает `git add` -> `git commit` -> `git push origin main`.
6. **Обновление прода:** Продагент выполняет `git pull origin main` и перезапуск нужного сервиса по регламенту.
7. **Проверка прода:** Продагент проверяет статус сервиса, доступность страниц/API и сообщает Максу результат.
8. **Эскалация:** если на проде нужна правка кода, Продагент не правит вручную, а возвращает задачу Forge в песочницу.

**Инфраструктура:**
*   **Server Backend:** Ubuntu 22.04 LTS (IP: 23.172.217.180).
*   **Server DB:** Ubuntu 22.04 LTS (IP: 83.217.220.97).
*   **Web Server:** Uvicorn (App) + Python http.server (Static Frontend) -> *В будущем Nginx*.
*   **Process Manager:** Systemd (fastapi.service, frontend.service).

**Backend:**
*   **Language:** Python 3.12+
*   **Framework:** FastAPI
*   **Database Driver:** psycopg2-binary
*   **Security:** python-dotenv (хранение секретов в .env), CORS.

**Frontend:**
*   **Type:** Static HTML/JS/CSS (SPA-like).
*   **Interaction:** Fetch API -> JSON.

**Database:**
*   **System:** PostgreSQL 14+

### Окружение и запуск

Чтобы любой (в т.ч. AI) мог знать всё о сервере и проекте без лишних вопросов:

*   **Путь к проекту на сервере:** `/home/makc/Apps/island`
*   **Запуск приложения:** служба `fastapi.service` стартует так (рабочая директория — корень проекта):
    ```bash
    /usr/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
    ```
    Команды перезапуска и логов — ниже в блоке «Пульт управления». При желании их можно заранее проверить вручную.
*   **Переменные окружения (.env):** в корне проекта файл `.env` (не коммитить). Имена переменных: `DB_HOST`, `DB_USER`, `DB_PASS`, `DB_NAME` — их читает `main.py` для подключения к PostgreSQL. Опционально: `DB_SSLMODE` (например `require` или `prefer`). При ошибке «SSL connection has been closed unexpectedly» попробуй `DB_SSLMODE=require`; при `verify-full` проблемы с сертификатом могут вызывать разрывы.
*   **Ограничения и производительность:** приложение и БД на разных хостах (сервер приложения и сервер БД — см. выше). Пула соединений к БД нет: каждый запрос к API создаёт новое подключение к PostgreSQL. При росте нагрузки имеет смысл добавить пул соединений.
*   **Unit-файлы (файлы служб systemd):** лежат в `/etc/systemd/system/` — например `fastapi.service`, при необходимости `frontend.service`. Чтобы посмотреть путь к конкретной службе: `systemctl show fastapi.service -p FragmentPath`.

### Резервное копирование БД

*   **Папка:** `db_backups/` в корне проекта.
*   **Скрипт:** `db_backups/bu_db.sh` — по расписанию (cron) сравнивает текущий размер БД с последним сохранённым; если размер изменился, создаёт дамп `default_db_YYYYMMDD_HHMMSS.dump` (формат `pg_dump -F c`).
*   **Лог:** `db_backups/backup.log` — дата и имя созданного файла или «размер не изменился, бэкап пропущен».
*   **Восстановление / перенос:** последний файл `*.dump` в `db_backups/` можно использовать для восстановления на этом сервере или для переноса на другой (например, при миграции на Timeweb): `pg_restore -h ... -U ... -d ... -F c /path/to/latest.dump`. Учитывать, что дамп может быть на несколько часов отставать от текущего состояния БД (если cron раз в сутки).

---

## Таблицы

Подробное описание таблиц БД см. в [Readme/tables.md](tables.md).

Структура и логика личного кабинета (хедер, контент, футер; **шаги в Мечтах и Расписание** — один источник данных, особенности финцели в Расписании) — в [Readme/structure.md](structure.md). **Лайки на витрине** (избранное, счётчик, уведомление владельцу «Вашу мечту добавили в избранное») и таблица уведомлений `dream_favorite_notifications` описаны в [Readme/structure.md](structure.md) (§ Лайки на витрине) и [Readme/tables.md](tables.md) (§ 9.1).

| Таблица | Назначение |
|--------|------------|
| **users** | Пользователи: имя, фамилия, телефон, город, хеш пароля. |
| **dreams** | Мечты пользователей: текст, дедлайн, цена, связь со статусом и категорией. |
| **dreams_steps** | Шаги к мечте (чек-лист): название, выполнено/нет, порядок. |
| **dream_statuses** | Справочник статусов мечты: Запланировано 💡, В работе ☭, Выполнено ✅. |
| **dreams_categories** | Справочник категорий мечты: Здоровье ❤️, Финансы 💰, Отношения 💑, Благотворительность 🤝, Проекты 📋, Развитие 📚. |

---

## Хедер личного кабинета (источник данных)

Все значения в хедере берутся только из БД (без заглушек).

| Элемент | Источник | Описание |
|--------|----------|----------|
| **Инициалы / Имя** (например: СЩ, Света Щербинина) | **users** | `name`, `surname`; инициалы — первые буквы имени и фамилии. |
| **N мечт всего** | **dreams** | Количество записей `dreams` с `user_id` текущего пользователя. |
| **Сбылось: N мечт \| M раз** | **dreams_log** | По мечтам пользователя: число уникальных мечт с хотя бы одной записью в `dreams_log` и общее число записей («раз»). |
| **1 бадди** / **0 бадди** | **users** | Наличие бадди: у текущего пользователя заполнено поле `buddy_id` (1 бадди) или нет (0 бадди). |
| **исполнил / N / мечту, мечты, мечт** (три строки) | **dreams_log** | Число записей в `dreams_log`, где `fulfilled_by_user_id` = текущий пользователь и мечта принадлежит другому. Строки: «исполнил», число N, склонение: 1 мечту, 2–4 мечты, 0/5+ мечт (правила в `.cursor/rules/header-helped-declension.mdc`). |

Данные для мечт и счётчиков «сбылось» / «чужих исполнил» приходят в ответе API `GET /dreams?user_id=...`: поля `dreams`, `dreams_fulfilled_count`, `dreams_fulfilled_times`, `dreams_fulfilled_by_me`. Бадди и имя — из ответа `POST /login` (текущий пользователь).

---

## Что сделано (23.02.2025)

**Для Атласа (и для онбординга):** **Техстек:** backend — Python 3.12+, FastAPI, psycopg2-binary, dotenv; frontend — статический HTML/JS/CSS, Fetch API; БД — PostgreSQL 14+; прод — Ubuntu, systemd unit `island` (uvicorn), Nginx, SSL (Let's Encrypt). Детали окружения и security — [Readme/atlas-status-report.md](atlas-status-report.md) (islanddream.ru, 188.225.44.48).

**Изменения за день:**

1. **Поиск и фильтр в модалке «Добавить бадди»**
   * **БД:** колонка `users.gender` (значения `'m'` / `'f'` или NULL). Миграция: `_sql/mig_users_gender.sql` (применить: `python3 run_migrate.py _sql/mig_users_gender.sql`).
   * **API:** `GET /users/list` возвращает поле `gender`. Если колонки ещё нет — ответ без падения, у всех `gender: null`.
   * **Фронт:** в модалке две строки: первая — кнопки фильтра **Все | М | Ж**, вторая — иконка лупы + поле «Поиск по имени или фамилии». Фильтрация и поиск по уже загруженному списку (без доп. запросов). Активная кнопка: «Все» — зелёный, «М» — синий, «Ж» — розовый. Рамка только у поля поиска (без двойной обводки блока).
   * Документация: [Readme/tables.md](tables.md) (колонка `gender`), [Readme/migrations_applied.md](migrations_applied.md).

2. **Удаление пользователя из БД**
   * Раньше при удалении пользователя в DBeaver возникала ошибка FK: мечты (`dreams.user_id`) ссылаются на `users.id`.
   * **Миграция:** `_sql/mig_dreams_user_cascade.sql` — внешний ключ `dreams.user_id` переведён на `ON DELETE CASCADE`: при удалении пользователя его мечты удаляются автоматически. Применить: `python3 run_migrate.py _sql/mig_dreams_user_cascade.sql`.

3. **Деплой (проверенный процесс)**
   * **Песок (локально):** `git add . && git commit -m "..." && git push origin main`
   * **Прод:** `git pull origin main && sudo systemctl restart island`
   * На проде перед `pull` желательно проверить `git status` (нет ли локальных правок). Если есть миграции — выполнить их после `pull`, затем рестарт. Служба на проде: **island** (не fastapi).

4. **Разовые и повторяющиеся шаги (18.03.2026)**
   * Модалка «Добавить шаг»: поле «Дата шага» (всегда), галочка «Повторять шаг». При выключенной галочке — блок «Дата начала / Дата окончания / Дни недели» серый и неактивен. Разовый шаг = название + дата (1 шаг). Повторяющийся = период + дни недели (много шагов). Roadmap №20.

5. **Форж самостоятельно вносит изменения в БД (18.03.2026)**
   * В Cursor включён Auto-Run (Settings → Cursor Settings → Agents). Агент может запускать `run_migrate.py` и другие Python-скрипты без подтверждения. Тест: создана таблица `_agent_test_log`, миграция выполнена успешно. Roadmap №33.

6. **Roadmap в БД, мобильная версия, добавление идей (15.03.2026)**
   * **БД:** таблица `roadmap` (step, text, section, status, initiator, count, date_added, date_done, priority, comment). Миграция mig_roadmap — создана, применена, данные загружены из roadmap.html.
   * **API:** `GET /roadmap` (фильтры status, section), `POST /roadmap` (добавить идею).
   * **roadmap.html:** данные загружаются из БД; мобильная версия — карточки с №, Текст, Раздел, Статус (только иконки), Инициатор; форма «+ Добавить идею» в обеих версиях.

7. **Модалка «Добавить шаг»: новый UI (15.03.2026)**
   * «Дата» заменено на «Выполнить до». Два блока в ряд: «Выполнить до» + дата, справа — кнопка «Повторять». При нажатии «Повторять» блок даты неактивен, блок «Дата начала / окончания» и дни недели активны. Дни недели — кнопки (Пн ВТ СР ЧТ ПТ СБ ВС) вместо чекбоксов. Выравнивание по левому краю, без переполнения.

8. **Документ личности Форжа (07.04.2026)**
   * В репозиторий добавлен [Readme/Forge.md](Forge.md): восстановлен контекст из `/home/makc/Backup/save/Forge` (README, `_identity/forge_personality.md`, семья AI). В списке освежения контекста для ассистента — ссылка на Forge.md.

9. **Разделение ролей Forge и Продагента (07.04.2026)**
   * В `Readme/Readme.md` добавлен явный блок «Разделение агентов: локалка и прод». Зафиксировано, что Forge работает в локальной песочнице `~/Apps/island`, а Продагент — на прод-сервере; при чтении Readme на сервере агент должен идентифицировать себя как Продагент.

10. **Контракт Остров ↔ Bridge (Bloom), канон в отдельном репо (09.04.2026)**
   * Спецификация: [island-bridge-contract](https://github.com/makcbarabanov/island-bridge-contract) (`api.md`, `db-shared.md`, `runbook.md` и т.д.); зеркало: `bridge/docs/handbook/` в репозитории бота. Согласование изменений API — сначала `api.md` / при необходимости `db-shared.md`, затем код на Острове и в боте. Пуши с общего аккаунта Макса; отдельный collaborator на GitHub пока не требуется.

11. **Проверка роли агента по IP (07.04.2026)**
   * В `Readme/Readme.md` добавлена обязательная проверка окружения перед работой: если хост/сервер имеет IP `188.225.44.48`, агент автоматически принимает роль Продагента; в локальной песочнице `~/Apps/island` — роль Forge.

12. **Взаимоисключающие роли агентов (07.04.2026)**
   * В `Readme/Readme.md` добавлено жёсткое правило: роли Forge и Продагента не смешиваются. Локально агент всегда только Forge, на IP `188.225.44.48` всегда только Продагент.

13. **Продагент без структурных правок (07.04.2026)**
   * Зафиксировано разделение контура: Продагент на сервере не делает рефакторинг и «наведение порядка» в кодовой базе. Такие изменения делает Forge в локальной песочнице, затем через GitHub изменения попадают на прод через `git pull`.

14. **SOP Forge ↔ Продагент (07.04.2026)**
   * Добавлен короткий регламент из 8 шагов: постановка задачи, проверка роли, локальная разработка Forge, публикация в Git, обновление и проверка прода Продагентом, правило эскалации без ручных правок кода на проде.

15. **Разные цвета окон Cursor для ролей (07.04.2026)**
   * В `/.vscode/settings.json` для локального окна Forge задан светло-синий фон и обновлён заголовок окна (`FORGE LOCAL — ${rootName}`), чтобы визуально не путать локальную песочницу с прод-окном.

16. **Возврат к тёмной теме Forge с синей подсветкой (07.04.2026)**
   * По запросу Макса `/.vscode/settings.json` возвращён к тёмной схеме (как было вначале): тёмный фон редактора и синяя подсветка статус/тайтл-бара для быстрого распознавания локального окна Forge.

17. **Песочная подсветка локальной песочницы (07.04.2026)**
   * Для окна локальной песочницы в `/.vscode/settings.json` синий акцент заменён на песочный (status/title bar), заголовок окна обновлён на `FORGE SANDBOX — ${rootName}`.

18. **Стратегия важнее тактики для агентов (07.04.2026)**
   * Добавлено правило: если в ходе тактической задачи обнаружена системная проблема, агент останавливает текущую тактику и предлагает системное решение. Если системно решить нельзя (границы доступа/безопасности/компетенции), агент обязан явно сообщить об ограничении и причине.

19. **Унификация путей проекта на `/home/makc/Apps/island` (07.04.2026)**
   * Старые упоминания `/home/makc/app` заменены на `/home/makc/Apps/island` в актуальных документах и service-шаблонах (`Readme/Readme.md`, `DEPLOY.md`, `Readme/atlas-status-report.md`, `Readme/sync-prod-with-sandbox.md`, `island.service`, `ostrov.service`, `db_backups/bu_db.sh`). Исторический лог `db_backups/backup.log` не переписывался.

20. **Команды пуша `island` / `контракт` (10.04.2026)**
   * Зафиксирован рабочий процесс: `bridge` не входит в автокоманды для Форжа; команды «запуш контракт», «запуш island», «запуш оба» относятся только к репозиториям ОСТРОВа и контракта. В README контракта добавлено правило пометки автора изменений: «Автор: Форж» / «Автор: Бридж».

21. **Правило для папки `errors/` (10.04.2026)**
   * После устранения ошибки агент обязан предложить Максу удалить связанный скрин/файл ошибки из `errors/`, чтобы не засорять директорию. Удаление выполнять только после явного подтверждения Макса.

22. **Фикс ошибки JSON в настройках профиля (10.04.2026)**
   * В `index.html` усилена обработка ошибок для сохранения профиля и загрузки аватара: если сервер вернул не JSON (например HTML-страницу ошибки), интерфейс больше не падает с `Unexpected token '<'`, а показывает понятное сообщение пользователю.

23. **Упрощение финцелей и книг до общего режима (10.04.2026)**
   * Спец-правила вынесены в архив: [Readme/archive-special-rules.md](archive-special-rules.md). В UI отключён спец-режим таблицы финцели (показываются обычные шаги), в API расписания отключено добавление книжных спец-элементов (только обычные шаги). Старый код сохранён через флаги для возможного возврата после отдельного тестирования.

24. **Возврат доступа к песочнице на `localhost:8000` (10.04.2026)**
   * В `docker-compose.yml` для сервиса `app` возвращён проброс порта `8000:8000`. После перезапуска контейнера песочница снова открывается по `http://localhost:8000/`.

25. **Фикс загрузки мечт в песочнице Docker (10.04.2026)**
   * Исправлена связка `app` ↔ `db` в docker-compose (явно задан `DB_PASS` для `app` под текущую Docker-БД). Для песочницы выполнена инициализация схемы из `init.sql`. В `main.py` добавлен безопасный `rollback` в fallback-путях загрузки шагов/книг, чтобы не залипала транзакция при частично несовместимой схеме. После фикса `GET /dreams` снова отвечает `200`.

26. **Синхронизация песочной БД с дампом (10.04.2026)**
   * В Docker-песочницу восстановлен `db_backups/default_db_20260319_030003.dump` (клиент `pg_restore` из `postgres:17`). Перед восстановлением сохранён откатный дамп `sandbox_before_restore_2026-04-10_11-14-14.dump`. После восстановления в песке появились актуальные таблицы/поля (`users.avatar_path`, `telegram`, `vk`, `buddy_id`, `buddy_trust`) и рабочие данные (`users=62`, `dreams=237`), `GET /dreams?user_id=1` отвечает `200`.

27. **Безопасная схема синхронизации песка и прода (10.04.2026)**
   * Обновлён регламент [Readme/sync-prod-with-sandbox.md](sync-prod-with-sandbox.md): код и БД разделены, релизы на прод только по `prod-safe` gate, деплой через `git pull --ff-only` + smoke-check, откат через `reflog/reset`, синхронизация данных БД только отдельным дампом через Продагента.

28. **Счётчики для повторяющихся шагов (10.04.2026)**
   * В `index.html` добавлена display-логика для повторяющихся шагов: одинаковые названия внутри одного списка шагов теперь показываются как `Название (n/total)` (например `Энергопрактика ежедневно (1/21)`), без изменения данных в БД.

29. **Фильтры шагов в едином стиле меню (10.04.2026)**
   * Блок фильтров «Все / Текущие / Просроченные / Выполненные / Удалённые» переведён с чекбоксов на кнопки-пункты меню в стиле фильтров мечт; активный фильтр подсвечивается, одновременно активен только один фильтр.

30. **Выравнивание визуального стиля фильтров шагов и мечт (10.04.2026)**
   * Исправлен рендер фильтров в разделе «Шаги»: добавлены те же визуальные слои (`dreams-filter-bg` + `dreams-filter-text`), сокращённые подписи и переопределение ширины кнопок (`width:auto`) для устранения вертикального «списка» из-за глобального стиля `button`.

31. **Фильтры шагов в одной строке с заголовком (10.04.2026)**
   * В разделе «Шаги» удалён текст «Все шаги», фильтры (`📋Все`, `☭Сейчас`, `⏰Проср.`, `✅Готово`, `🗑️Удал.`) перенесены в ту же строку, что и заголовок «Шаги», по аналогии с блоком «Мои мечты».

32. **Фильтры шагов перенесены в верхнюю строку раздела (10.04.2026)**
   * Блок фильтров шагов вынесен из `steps-block` в общий верхний ряд `dreams-title-row` рядом с `app-section-title`; теперь при переключении меню показывается либо фильтр мечт, либо фильтр шагов в одной и той же строке без дубля заголовка.

33. **Для шагов добавлено время «с–по» (10.04.2026)**
   * В API шагов добавлены поля `start_time` и `end_time` (формат `HH:MM`, без секунд).
   * В БД `dreams_steps` добавлены колонки времени (`TIME`) миграцией.
   * В модалке создания шага добавлены поля «Время с» и «Время по», а в списках шагов время показывается мелким шрифтом под датой.

34. **Плейсхолдер времени в шагах (10.04.2026)**
   * Для шагов без заполненного времени теперь всегда показывается строка `с --:-- по --:--`, чтобы блок выглядел как расписание дня даже до установки времени.

35. **Ввод времени шагов только в формате 24 часа (10.04.2026)**
   * В модалке шага убран нативный `AM/PM` time-picker; поля времени переведены на формат `HH:MM`.
   * Добавлена валидация диапазона `00:00–23:59` и автонормализация (например `8:15` -> `08:15`).

36. **Фикс создания шага с временем (10.04.2026)**
   * Исправлена ошибка `ReferenceError: timeFromIn is not defined` в обработчике кнопки «Создать» в модалке шагов.
   * Восстановлено создание шагов (одиночных и повторяющихся) с заполнением времени.

37. **Светло-серые плейсхолдеры времени в шагах (10.04.2026)**
   * Для незаполненного времени строка `с --:-- по --:--` оставлена как необязательный плейсхолдер, но выделена отдельным светло-серым цветом, чтобы отличаться от реального времени шага.

38. **Фикс ширины модалки и формата дат в шагах (10.04.2026)**
   * Модалка «Добавить шаг» переведена на фиксированную ширину, чтобы окно не «прыгало» при выборе дат.
   * Поля дат в модалке шага переведены на формат `dd.mm.yyyy` (`24.04.2026`) с валидацией и конвертацией в API-формат `YYYY-MM-DD`.

39. **Календарик возвращён, время снова необязательное (10.04.2026)**
   * В полях дат шага возвращён нативный календарь (`type=date`), при этом отображение даты остаётся в локальном формате интерфейса.
   * Поля времени `Время с` и `Время по` снова полностью необязательны: можно не заполнять или заполнить только одно из них.

40. **Формат дат шага `dd.mm.yyyy` и возврат подсказок (10.04.2026)**
   * Поля дат в модалке шага снова работают в явном формате `dd.mm.yyyy` (через точку, начиная с дня), без `mm/dd`.
   * Возвращена подсказка под полями времени: `Время необязательно, формат HH:MM (24 часа)`.

41. **Strict Scope по умолчанию (10.04.2026)**
   * Зафиксирован стандарт работы: агент делает только явно разрешённые правки.
   * Любые сопутствующие изменения (UI/UX, тексты, поведение, стили, рефакторинг «заодно») сначала предлагаются отдельным списком и выполняются только после явного одобрения Макса.
   * Режим «Только по ТЗ, без сопутствующих правок» принят как дефолт, а не как разовая команда.

42. **Возвращены календарик и подсказки в модалке шага (10.04.2026)**
   * Для полей дат `Выполнить до`, `Дата начала`, `Дата окончания` снова включён нативный календарь (`type=date`).
   * В модалке добавлена подсказка по дате: нажать на поле для открытия календаря; подсказка по времени сохранена.

43. **Вариант 2 по датам: `dd.mm.yyyy` + кнопка календаря (10.04.2026)**
   * В модалке шага поля дат снова зафиксированы в формате `dd.mm.yyyy`.
   * Для выбора даты через календарь добавлена кнопка `📅` рядом с каждым полем даты (`Выполнить до`, `Дата начала`, `Дата окончания`).

44. **Выравнивание кнопки «Повторять» в одной строке с датой (10.04.2026)**
   * Исправлены отступы/высота служебного спейсера в строке режима модалки шага, чтобы кнопка `Повторять` была на одном уровне с полем даты слева.

45. **Кнопка редактирования шага рядом с удалением (10.04.2026)**
   * В списке «Шаги» для обычных шагов добавлен `✎` рядом с `✕`.
   * Нажатие на `✎` открывает редактирование названия шага (inline), чтобы редактирование было доступно через отдельный явный контрол.

46. **Усилена видимость кнопки редактирования шага (10.04.2026)**
   * Для кнопки редактирования в списке «Шаги» увеличена видимая ширина и контраст.
   * Символ заменён на более заметный `✏️`, чтобы контрол не терялся рядом с `✕`.

47. **Карандаш добавлен в шаги внутри «Мои мечты» (10.04.2026)**
   * Кнопка `✏️` добавлена не только в общий список «Шаги», но и в строки шагов внутри блока «Мои мечты» (desktop и mobile карточки), рядом с `✕`.
   * Кнопка открывает inline-редактирование названия шага.

48. **Редактирование шага через модальное окно (10.04.2026)**
   * По нажатию на `✏️` теперь открывается модалка шага в режиме редактирования (на базе окна создания шага): можно изменить название, дату и время `с/по`.
   * В режиме редактирования кнопка действия меняется на `Сохранить`, а повторяющее создание шагов отключено.

49. **Системное выравнивание кнопки «Повторять» (10.04.2026)**
   * В строке режима модалки шага (`дата`/`Повторять`) убран хрупкий спейсер и введена единая структура: общий класс лейбла + контейнер поля фиксированной высоты.
   * Кнопка `Повторять` теперь выравнивается по той же вертикальной сетке, что и поле даты, и не «убегает» между режимами создания/редактирования.

50. **Единый baseline для даты и «Повторять» (10.04.2026)**
   * Для контейнеров поля даты и кнопки `Повторять` зафиксировано выравнивание по нижней границе (`flex-end`) при одинаковой высоте строки поля.
   * Это убирает смещение кнопки вверх относительно соседнего поля даты.

51. **Жёсткое выравнивание control-row даты и кнопки (10.04.2026)**
   * Для `дата` и `Повторять` включён режим `stretch` в контейнерах control-row при общей высоте 38px.
   * Кнопка `Повторять` растягивается на 100% высоты своего контейнера, чтобы совпадать по вертикали с соседним полем даты без ручных пиксельных сдвигов.

52. **Временная отладочная обводка блоков модалки шага (10.04.2026)**
   * Для диагностики вёрстки добавлены цветные dashed-обводки на контейнеры строки `Выполнить до / Повторять` (`single-block`, `repeat-btn-block`, `date-field`, `repeat-btn-wrap`).
   * Нужны только для визуального анализа геометрии; после локализации проблемы должны быть удалены.

53. **Сброс скрытых отступов в control-row даты/повторения (10.04.2026)**
   * Для поля даты, кнопки календаря и кнопки `Повторять` зафиксированы `margin:0`, единая высота `38px`, `box-sizing:border-box` и блочный рендер внутри контейнера строки.
   * Это убирает влияние скрытых внутренних отступов/line-height, из-за которых кнопка визуально «плыла» вверх/вниз.

54. **Удалены временные отладочные бордеры (10.04.2026)**
   * Из модалки шага убраны диагностические dashed-обводки контейнеров строки `Выполнить до / Повторять` после проверки геометрии.

55. **RFC по редактированию повторяющихся шагов (10.04.2026)**
   * Подготовлен проектный RFC: [Readme/steps-editing-rfc.md](steps-editing-rfc.md).
   * Зафиксированы: модель серии шагов, `scope` для PATCH (`single/from_current/all_series`), UX выбора области применения, edge cases и поэтапный план внедрения.

---

## 📔 Журнал проекта
*   Файл `journal.txt` — хронология действий участников (дата, время, автор, действие). Внутренний лог без возможности отката, но с учётом истории.

---

## 🤝 ПРАВИЛА ВЗАИМОДЕЙСТВИЯ

1.  **Обращение:** Всегда обращаться к тебе на «ты» (не на «вы»).
2.  **Единственный источник правды — БД.** Никаких «заглушек» и изменений только на фронтенде без явного указания с твоей стороны. Все данные на сайте берутся из базы данных. Если просишь изменить что-то на фронте (например, смену статуса или категории мечты), это подразумевает и сохранение в БД, и отображение актуальных данных из БД после обновления. Фронтенд не хранит состояние «только в рамках сессии» — любое действие пользователя, меняющее данные, должно попадать в БД и при перезагрузке страницы подтягиваться оттуда.
3.  **Пропорции пространства (мобильная и общая вёрстка):** Главному контенту (в кабинете — сами мечты, карточки/лента мечт) отводится максимально возможное место. Хедер, меню, вторичные кнопки — на периферию: компактно, без лишней доли экрана. На мобильной версии приоритет особенно важен: не допускать, чтобы хедер и меню занимали непозволительно большую долю экрана в ущерб мечтам.
4.  **Strict Scope (по умолчанию):** Агент выполняет только явно запрошенный объём. Любые сопутствующие/дополнительные правки запрещены без предварительного уведомления и явного одобрения Макса.

---

## 🔒 Правила разработки (Rules)
1.  **Безопасность:** Файл `.env` никогда не попадает в Git. Пароли в базе не храним открытым текстом (в будущем подключим хеширование).
2.  **Git Flow:** Изменения сначала тестируются локально или на сервере, затем фиксируются (`git commit`) и отправляются в репозиторий (`git push`).
3.  **Code Style:** Чистый код, понятные имена переменных, обработка ошибок через `try/except`.
4.  **Документация БД:** При любом изменении схемы или данных в БД (миграции, новые таблицы/колонки/справочные значения) сразу вносится соответствующая запись в [Readme/tables.md](tables.md).
5.  **Обновление Readme:** После значимых изменений (фичи, баги, UI, БД) — сам обновлять Readme/Readme.md (раздел «Что сделано»), tables.md (при изменении схемы). Roadmap — в БД (таблица `roadmap`), изменения через roadmap.html или API. Правило: [.cursor/rules/update-readme.mdc](.cursor/rules/update-readme.mdc).
6.  **Телефоны в БД:** Единый формат — без пробелов, слитно: `+79998880011`. Нормализация: скрипт `scripts/normalize_phones_and_passwords.py` (приводит к +7…, при необходимости задаёт пароль «последние 4 цифры» пользователям без пароля).
7.  **Миграции и папка _sql:** Выполненная миграция (через `run_migrate.py`) самоликвидируется — файл удаляется из `_sql/`. Проверить, делал ли пользователь миграцию, можно по наличию файла в `_sql/`: нет файла — миграция уже выполнена; файл есть — нужно выполнить.
    *   **Агент (Forge) умеет выполнять миграции сам.** После добавления или правки SQL-файла в `_sql/` агент должен сам запустить `python3 run_migrate.py _sql/имя_файла.sql` (из корня проекта, где есть `.env`). Не перекладывать эту операцию на пользователя: одна команда в терминале — часть задачи «внести изменения в БД».
    *   Если пользователь пишет «перезапустил, результата не вижу» и речь про изменения из миграции — напомнить: сначала выполнить миграцию (или выполнить её самому, если файл ещё в `_sql/`), затем перезапуск сервиса.
    *   **405 Method Not Allowed** при создании шагов финцели (кнопка «Создать шаги» в модалке): обычно значит, что на сервере (23.172.217.180) запущена старая версия backend без маршрута `POST /dreams/{id}/steps/finance`. Нужно задеплоить актуальный код и перезапустить службу: `sudo systemctl restart fastapi.service`.
8.  **Стратегия важнее тактики:** Если в процессе выполнения текущей задачи выявлена системная проблема (повторяющаяся, архитектурная, инфраструктурная, прав доступа), агент должен приостановить тактическое выполнение и предложить системное решение. Если упирается в границы (безопасность, права, ограничения среды), агент обязан явно уведомить Макса, что системное решение сейчас недоступно, и указать точную причину/ограничение.
9.  **Папка `errors/`:** После фикса ошибки всегда предложить удалить соответствующий скрин/артефакт из `errors/` для чистоты директории; удалять только после подтверждения пользователя.
10. **Деплой на прод:** Из песка в прод уходит только `prod-safe` код через Git (`git pull --ff-only`). Данные БД не синхронизируются через Git; обновление песка данными прода — только отдельным дампом и отдельной процедурой с бэкапом.
11. **Уточняющие вопросы обязательны:** Если задача сформулирована неясно, есть спорный момент или возможны разные трактовки — сначала задать уточняющие вопросы, затем выполнять изменения.

**Опыт: почему агент раньше не запускал миграции сам.** В инструкциях было сформулировано так: «напомнить пользователю выполнить миграцию» / «файл есть — нужно выполнить» — без явного «агент может и должен выполнить сам». Агент следовал букве: предлагал команду и ждал действий пользователя. После того как пользователь один раз попросил «дай команду на миграцию» и агент её выполнил, стало ясно: (1) у агента есть доступ к терминалу и к `.env` в корне проекта, `run_migrate.py` успешно отрабатывает; (2) выполнение миграции — не «особо опасная» операция, требующая только ручного подтверждения, а часть обычного цикла «написал SQL → применил к БД»; (3) явная фиксация в Readme («агент умеет и должен выполнять миграции сам») убирает неопределённость и в следующий раз агент будет запускать миграцию по умолчанию, не перекладывая на пользователя. **Вывод на будущее:** если в проекте есть повторяемая операция (миграция, скрипт, сборка), и у агента есть все данные и инструменты для её выполнения — явно записать в правила/Readme, что агент делает её сам в рамках задачи, если пользователь не попросил иначе.

🕹️ ПУЛЬТ УПРАВЛЕНИЯ (Шпаргалка)

**Единый порт приложения: 8000.** Витрина и личный кабинет открываются с одного адреса (FastAPI отдаёт и HTML, и API). До деплоя на продакшен порт не меняем.
* Корень = личный кабинет, лендинг: http://23.172.217.180:8000/ или http://23.172.217.180:8000/index.html
* Публичная витрина (без авторизации): http://23.172.217.180:8000/dreams.html — просмотр. Захотел помочь → регистрация в ЛК. В ЛК витрина тоже есть (клик «0 помог» или «💫 Витрина мечт»).

Все команды выполняются в терминале сервера.
1. БЭКЕНД (API + HTML, Порт 8000)

Служба называется: fastapi.service

    Перезапустить (после любых изменений в main.py):
    code Bash

    sudo systemctl restart fastapi.service

    Посмотреть статус (работает или упал?):
    code Bash

    sudo systemctl status fastapi.service

    Читать логи в живом времени (если вылезла ошибка 500):
    code Bash

    sudo journalctl -u fastapi.service -f

    (Чтобы выйти из логов, нажми Ctrl + C).

2. ФРОНТЕНД (HTML, Порт 8080)

Для ОСТРОВ открывай сайт с порта 8000 (бэкенд отдаёт HTML). frontend.service здесь не обязательна.

Служба называется: frontend.service. Для ОСТРОВ используй порт 8000.

    Перезапустить (обычно не нужно, если меняешь только HTML, но на всякий случай):
    code Bash

    sudo systemctl restart frontend.service

    ВАЖНО ПРО БРАУЗЕР:
    Браузеры очень любят кэшировать (запоминать) старые версии сайтов.
    Если ты изменил index.html, но не видишь изменений — нажми в браузере:
    Ctrl + F5 (или Cmd + Shift + R на Mac). Это "жесткое обновление".

🚑 АВАРИЙНЫЙ РЕЖИМ (Если порт занят)

Если ты случайно запустил сервер вручную (просто написав uvicorn... или python3 -m http...) и закрыл терминал, этот процесс может «зависнуть» и занимать порт. Тогда служба systemd не запустится (ошибка Address already in use).

Лечение (Убить ручные процессы):
code Bash

# Убить всех ручных "питонов", которые держат веб-сервера
pkill -f uvicorn
pkill -f http.server

После этого снова сделай restart нужной службы.
Твой Алгоритм Работы с Форжем:

    Форж меняет код в main.py.

    Ты в терминале пишешь: sudo systemctl restart fastapi.

    Форж меняет код в index.html.

    Ты в браузере жмешь Ctrl + F5.

Всё! Больше никаких ручных запусков. Пусть работают роботы (systemd).

---

## 🖥 Локальный запуск (разработка)

На своей машине (или для теста без systemd):

1. **Backend:** в корне проекта создай `.env` с переменными (хост БД, пароль и т.д.), затем:
   ```bash
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
   ```
2. **Сайт:** в браузере открывай `http://localhost:8000/` (витрина) или `http://localhost:8000/index.html` (кабинет).

Не коммить `.env` в Git.

---

## 📎 Связанные документы

| Файл | Описание |
|------|----------|
| [Readme/tables.md](tables.md) | Схема БД, таблицы, поля. Обновляется при любых изменениях в БД. |
| [Readme/atlas-status-report.md](atlas-status-report.md) | **Для Атласа:** прод (islanddream.ru), systemd `island`, Nginx, SSL, binding 127.0.0.1, команды hardening. |
| [Readme/migrations_applied.md](migrations_applied.md) | Справочник применённых миграций (копии SQL из `_sql/` после выполнения). |
| `journal.txt` | Журнал проекта: дата, автор, действие. |
| `.cursor/rules/` | Правила для Cursor (склонения, статистика dreams_log и т.д.). |
